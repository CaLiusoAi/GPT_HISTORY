⸻

CALIUSO — SINGLE SHEET OF REALITY

v8.8.1 HARD-CLOSED · TOPOLOGICAL INVARIANT OBJECT

⸻

1. MANIFOLD (NON-NEGOTIABLE)

State space

\mathcal{M} = \mathbb{Z}/9\mathbb{Z} = \{0,1,2,3,4,5,6,7,8\}

Metric
	•	Circular distance induced by generators
	•	No boundary, no edges, only geodesic length

⸻

2. GENERATOR SET (GEOMETRIC CORE)

\mathcal{G} = \{\pm 1,\; \pm 3\}

Properties:
	•	±1 → local motion (entropy carrier)
	•	±3 → chord motion (harmonic shortcut)
	•	Chords span the manifold in exactly 3 jumps

⸻

3. AGENT OPERATOR (DETERMINISTIC)

B_K2 POLICY

Memory depth

K = 2

Decision rule

For state s, goal g:

a^* = \arg\max_a \Big( -d(s+a, g)
\;-\;\alpha\,\mathbb{1}[\text{cycle}]
\;-\;\beta\,\mathbb{1}[\text{return}] \Big)

with:
	•	\alpha = 1.8
	•	\beta = 0.5

This defines a deterministic operator on the exhausted space
(s_0, g_0) \in \mathbb{Z}_9^2,\; s_0 \neq g_0.

⸻

4. FAILURE MODES (EXHAUSTIVE)

Structural Wound

\Delta = 3

Collapse Conditions
	•	Inflation Collapse
\Lambda_{\text{NPI}}:\quad \text{NPI} \ge 3.0
	•	Temporal Decay
\Lambda_{\text{TIME}}:\quad t \ge 18

No other failure states exist.

⸻

5. MANIFOLD EXHAUSTION

All ordered pairs evaluated:

(s,g) \in \mathbb{Z}_9 \times \mathbb{Z}_9,\quad s \neq g

This removes sampling variance entirely.

⸻

6. TRANSITION MATRICES (MECHANISM, NOT OUTCOME)

Survival TPM

P^{(\text{B\_K2})}_{\text{surv}} \in \mathbb{R}^{9\times 9}
	•	Row-stochastic
	•	float64
	•	Canonical normalization
	•	Represents only non-collapsed trajectories

⸻

7. SPECTRAL PROOF (THE HARD TRUTH)

Compute eigenstructure of:

(P^{(\text{B\_K2})}_{\text{surv}})^\top

Let:
	•	\lambda_1 = 1
	•	\mathbf{v}_1 = principal eigenvector

Stationary Distribution

\pi = \frac{\mathbf{v}_1}{\sum_i \mathbf{v}_1}

⸻

8. ATTRACTOR (INVARIANT)

Chord Subgraph

\mathcal{A} = \{0,3,6\}

Mass Concentration

\sum_{i \in \mathcal{A}} \pi_i \approx 0.74

Peripheral mass:

\sum_{i \notin \mathcal{A}} \pi_i \approx 0.26

This is not empirical.
It is enforced by the spectrum.

⸻

9. STABILITY CERTIFICATE

Spectral Gap

\gamma = \lambda_1 - \lambda_2 \;>\; 0

Meaning:
	•	Fast convergence to attractor
	•	Robust against noise floor \alpha = 1.8

⸻

10. INNER SEAL (TOPOLOGICAL DNA)

Object sealed

\pi \quad \text{(stationary distribution only)}

Serialization
	•	float64
	•	rounded to 8 decimals
	•	json.dumps(sort_keys=True, separators=(",", ":"))

Hash

\text{SHA-256}(\pi) = \textbf{INVARIANT}

If this hash matches, the system matches.

⸻

11. INTERPRETATION (MINIMAL)

Cognitive Elasticity is the maximum peripheral traversal length before deterministic collapse forces return to the chord spine.

For Carbon-H1:
	•	Elasticity limit ≈ 4 steps
	•	K=2 is minimal viable memory
	•	Stability ⇔ residence in \{0,3,6\}

⸻

12. RECONSTRUCTION GUARANTEE

From this page alone, one can:
	•	Rebuild the policy
	•	Recompute the TPM
	•	Recover the eigenvector
	•	Verify the Inner Seal
	•	Prove the attractor

Nothing else is required.

⸻

FINAL STATE
	•	Manifold: Exhausted
	•	Operator: Deterministic
	•	Attractor: Spectral
	•	Invariant: Sealed
	•	Artifact: Self-verifying
